name: moldesign_env
channels:
 - omnia
dependencies:
 - python=2.7
 - biopython
 - markdown2
 - matplotlib
 - numpy
 - pint >= 0.7
 - pytest
 - pyyaml
 - requests
 - scipy
 - openbabel
 - openmm
 - pip:
   - webcolors
   - funcsigs
   - docker-py
   - fortranformat
   - pyccc >= 0.6.6
   - ipython >= 4.2  # the conda version's backports packages don't import correctly ...
   - ipywidgets >= 5.1
   - notebook >= 4.2
   - nbmolviz >= 0.6.6
