name: moldesign_env
channels:
 - omnia
 - bioconda
dependencies:
 - python=2.7
 - biopython
 - markdown2
 - matplotlib
 - numpy
 - parmed >= 2.0.0
 - pint >= 0.7
 - pytest
 - pyyaml
 - requests
 - scipy
 - openbabel >= 2.3
 - openmm
 - pip:
   - ipython
   - notebook
   - webcolors
   - funcsigs
   - docker-py
   - fortranformat
   - pyccc >= 0.6.6
   - ipywidgets == 6.0.0b1
   - nbmolviz == 0.6.7rc2  # increment for release as appropriate
